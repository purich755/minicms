/**
 * Типы таблиц БД.
 *
 * Заглушка до Фазы 1. После того как миграции применены, файл генерируется из схемы:
 *
 *   npx supabase gen types typescript --project-id <project-id> > lib/types.ts
 *
 * Затем клиенты Supabase параметризуются типом Database:
 *   createServerClient<Database>(...)
 *
 * Руками этот файл не правим — только перегенерируем.
 */

export {}
